apiVersion: apps/v1
kind: Deployment
metadata:
  name: docai-backend
spec:
  template:
    spec:
      containers:
        - name: docai-backend
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: production
