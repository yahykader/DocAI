# Staff Review — CI/CD Pipeline & Standards Setup (Module 1.B) — Round 3

**Feature Branch**: `006-cicd-standards`  
**Review Date**: 2026-05-31  
**Reviewer**: Staff Engineer (automated)  
**Spec**: `specs/006-cicd-standards/spec.md`  
**Review Timestamp**: 20260531-201948  
**Prior Reviews**: `review-20260531-213915.md` (Round 1 — APPROVED WITH CONDITIONS) · `review-20260531-220542.md` (Round 2 — CHANGES REQUIRED)

---

## Executive Summary

**Verdict**: ⚠️ APPROVED WITH CONDITIONS

All 3 blockers and warnings from Round 2 (N-001, N-002, N-003) are confirmed resolved. The `unleash-init` service now provisions exactly the 6 FR-019 flag names using a correctly scoped admin token — FR-019 is fully satisfied for the first time across all 3 review rounds. No new blockers have been introduced.

Two documentation warnings remain: the Phase 5 "Independent Test" header in `tasks.md` and the Étape 5 "Verification" block in `plan.md` both still contain the stale `kubectl apply --dry-run=client -f k8s/` command, which will fail now that `k8s/kustomization.yaml` exists in the directory. These should be corrected before merge to avoid developer confusion.

Spec coverage is 28/28 (100%).

---

## Prior Findings Status

| ID | Status | Evidence |
|----|--------|---------|
| F-001 (PIT skipped) | ✅ Fixed (Round 1) | `-DskipPit` removed from `01-ci.yml`; PIT runs in sonarcloud job |
| F-002 (catch Throwable) | ✅ Fixed (Round 1) | Both overloads use `catch (Exception e)` |
| F-003 (unleash:latest) | ✅ Fixed (Round 1) | `unleashorg/unleash-server:5.13.0` pinned |
| F-004 (no UNLEASH_API_TOKEN default) | ✅ Fixed (Round 1) | Default `*:*.unleash-insecure-api-token` in `application.yml` L243 |
| F-005 (flags not provisioned) | ✅ Fixed (Round 2 fix → Round 3 verified) | 6 correct FR-019 flag names confirmed; admin token used |
| F-006 (claude-api-key) | ✅ Fixed (Round 1) | Renamed to `openai-api-key` in `docs/secrets-rotation.md` |
| F-007 (staging-only K8s) | ✅ Fixed (Round 1) | Kustomize base + overlays (staging, production) |
| N-001 (wrong flag names) | ✅ Fixed | `billing.enabled`, `fraud.v2.enabled`, `extraction.mistral.enabled`, `dashboard.search.enabled`, `notifications.inapp.enabled`, `maintenance.mode` — exact FR-019 names confirmed at L349–354 |
| N-002 (client token for admin API) | ✅ Fixed | `INIT_ADMIN_API_TOKENS: "default:*.unleash-insecure-admin-token"` added to `unleash` service L315; `unleash-init` `UNLEASH_TOKEN` set to admin token L335 |
| N-003 (kubectl -f k8s/ broken) | ✅ Partially Fixed | `quickstart.md` + `tasks.md` T016 updated to `-k k8s/overlays/staging`; `tasks.md` Phase 5 header and `plan.md` Étape 5 still stale (see W-001, W-002) |

---

## New Findings Table

| ID | Severity | File | Line(s) | Description | Recommendation |
|----|----------|------|---------|-------------|----------------|
| W-001 | 🟡 Warning | `specs/006-cicd-standards/tasks.md` | L85 | Phase 5 "Independent Test" header still reads `kubectl apply --dry-run=client -f k8s/`. With `k8s/kustomization.yaml` present in the directory, this command fails: kubectl in `-f` mode does not recognize `kind: Kustomization` and errors with "no matches for kind 'Kustomization'". T016 (L98) was correctly updated but the phase-level Independent Test was not. | Replace with `kubectl apply --dry-run=client -k k8s/overlays/staging`. |
| W-002 | 🟡 Warning | `specs/006-cicd-standards/plan.md` | L307 | Étape 5 "Verification" block contains `kubectl apply --dry-run=client -f k8s/` with comment `# Expected: 3 resources configured (no errors)`. Same failure mode as W-001 — Kustomization resource in `k8s/` directory breaks `-f` mode. Plan.md is the primary technical reference document for this feature; a developer onboarding from it would encounter an immediate error. | Replace with `kubectl apply --dry-run=client -k k8s/overlays/staging`. |
| S-001 | 🟢 Suggestion | `specs/006-cicd-standards/tasks.md` | L111 | T017 task description specifies `image: unleashorg/unleash-server:latest` — F-003 correctly pinned this to `5.13.0` in the implementation, but the task's textual description was never updated. Minor inconsistency between spec intent and implementation documentation. | Update T017 description to reflect `unleashorg/unleash-server:5.13.0`. Non-blocking. |
| S-002 | 🟢 Suggestion | `specs/006-cicd-standards/plan.md` | L152 | Étape 2 job-mapping table shows `-DskipPit` in the `sonarcloud` row command. F-001 correctly removed this from the actual `01-ci.yml`, but `plan.md` was not updated. The plan now contradicts the live CI configuration. | Update Étape 2 table: remove `-DskipPit` from sonarcloud command row and rename the description to "Quality gates + PIT mutation testing + SonarCloud analysis". Non-blocking. |

---

## Spec Coverage Matrix (Complete)

| Requirement | Description | Status | Notes |
|-------------|-------------|--------|-------|
| FR-001 | 5-job CI pipeline with correct dependency graph | ✅ | Jobs: unit-tests, integration, bdd-tests, contract-tests, sonarcloud |
| FR-002 | Each job individually cancellable and reportable | ✅ | Separate jobs with independent permissions |
| FR-003 | Job 1 fails immediately on ArchUnit violation | ✅ | `mvn clean test -P unit-tests` stops on first failure |
| FR-004 | Jobs 2+3 activate container reuse | ✅ | `TESTCONTAINERS_REUSE_ENABLE=true` on both |
| FR-005 | ubuntu-latest, MAVEN_OPTS caps | ✅ | -Xmx512m jobs 1-4, -Xmx1g sonarcloud |
| FR-006 | Job 5 runs Checkstyle | ✅ | `quality-gates` profile includes Checkstyle |
| FR-007 | docai-domain ≥ 90% coverage | ✅ | PIT runs in CI (F-001 fixed) |
| FR-008 | Global coverage ≥ 80% | ✅ | PIT runs in CI (F-001 fixed) |
| FR-009 | SonarCloud blocks on new bugs | ✅ | SonarCloud quality gate configured |
| FR-010 | Methods ≤ 20 lines | ✅ | `MethodLength(max=20)` in checkstyle.xml |
| FR-011 | Methods ≤ 4 parameters | ✅ | `ParameterNumber(max=4)` in checkstyle.xml |
| FR-012 | Cyclomatic complexity ≤ 10 | ✅ | `CyclomaticComplexity(max=10)` in checkstyle.xml |
| FR-013 | Multi-stage Docker build (JRE 21 Alpine) | ✅ | 3-stage: dependencies, build, runtime |
| FR-014 | Container runs as non-root `docai` | ✅ | UID/GID 1001, USER docai |
| FR-015 | Trivy CRITICAL gate; HIGH reported only | ✅ | Two Trivy steps: exit-code 1 (CRITICAL) / 0 (HIGH) |
| FR-015b | Push to ghcr.io via GITHUB_TOKEN | ✅ | No extra credentials required |
| FR-015c | Production requires manual reviewer approval | ✅ | `environment: production` gate |
| FR-016 | K8s Deployment: resources, probes, rolling update | ✅ | startupProbe + livenessProbe + readinessProbe; maxUnavailable=0 |
| FR-017 | K8s Service exposes designated port | ✅ | ClusterIP on 8080 (+ 9091 management) |
| FR-018 | HPA with CPU triggers, min/max replicas | ✅ | min=2, max=10, CPU 70% |
| FR-019 | 6 feature flags configured in Unleash | ✅ | **RESOLVED Round 3**: `unleash-init` provisions `billing.enabled`, `fraud.v2.enabled`, `extraction.mistral.enabled`, `dashboard.search.enabled`, `notifications.inapp.enabled`, `maintenance.mode` via admin token — all default `false` |
| FR-019b | Unleash + PostgreSQL in docker-compose.yml | ✅ | unleash + unleash-db services present, image pinned to 5.13.0 |
| FR-020 | Per-tenant flag targeting | ✅ | `UnleashContext.builder().userId(tenantId)` |
| FR-021 | Runtime flag changes without restart | ✅ | Unleash polling interval 15s (SC-006) |
| FR-022 | Fail-safe: Unleash unavailable → false | ✅ | `catch (Exception e)` — returns false, never throws |
| FR-023 | Structured JSON logs with tenantId, traceId | ✅ | logback-spring.xml + TenantMdcFilter |
| FR-024 | traceId propagation; auto-generated if missing | ✅ | OpenTelemetry auto-instrumentation |
| FR-025 | Metrics endpoint (throughput, errors, JVM) | ✅ | Prometheus/Micrometer |
| FR-026 | Distributed tracing via OpenTelemetry | ✅ | micrometer-tracing-bridge-otel + OTLP to Tempo |
| FR-027 | Slow query logging in dev profile | ✅ | Configurable threshold, dev profile only |
| FR-027b | ADR-010 EXPLAIN PLAN manual PR checklist | ✅ | PR template includes complete checklist |

**Coverage**: 28 / 28 requirements fully implemented — **100%**

---

## Detailed N-001 / N-002 Verification

### N-001 — Flag Names (CONFIRMED FIXED)

`docker-compose.yml` L349–354:
```yaml
create_flag "billing.enabled"
create_flag "fraud.v2.enabled"
create_flag "extraction.mistral.enabled"
create_flag "dashboard.search.enabled"
create_flag "notifications.inapp.enabled"
create_flag "maintenance.mode"
```
Exact match with FR-019 and `quickstart.md`. Previously invented `docai.*` prefixed names are gone.

### N-002 — Admin Token (CONFIRMED FIXED)

`docker-compose.yml` L315 (unleash service):
```yaml
INIT_ADMIN_API_TOKENS: "default:*.unleash-insecure-admin-token"  # LOCAL PROFILE ONLY — admin token for flag provisioning (SEC-004)
```

`docker-compose.yml` L335 (unleash-init service):
```yaml
UNLEASH_TOKEN: "default:*.unleash-insecure-admin-token"
```

Admin token now present in both services. `POST /api/admin/projects/default/features` will receive the correct credentials (HTTP 200 instead of HTTP 403).

---

## Test Coverage Assessment

| Scope | Tests Present | Assessment |
|-------|--------------|-----------|
| `UnleashFeatureFlagAdapter` fail-safe (Exception) | ✅ | 2 tests — RuntimeException on both overloads returns false |
| `UnleashFeatureFlagAdapter` happy path (flag enabled/disabled) | ✅ | 4 tests — enabled/disabled for both overloads |
| `UnleashFeatureFlagAdapter` tenant mapping (userId in context) | ✅ | 1 test — ArgumentCaptor verifies `userId` = tenantId |
| `unleash-init` flag names correctness | ✅ Manual (T024) | No automated test; manual validation confirms names at runtime |
| Kustomize overlay validity | ✅ Manual (T016) | `kubectl apply --dry-run=client -k k8s/overlays/staging` required before merge |

---

## Security Assessment (Final)

| Check | Status | Notes |
|-------|--------|-------|
| All GitHub Actions `uses:` SHA-pinned (SEC-002) | ✅ | All 4 workflows verified: no `@v*`, no `@main` tags |
| Container runs as UID 1001 non-root (SEC-001) | ✅ | `adduser -u 1001 docai` in Dockerfile; `runAsUser: 1001` in K8s |
| No secrets in K8s manifests | ✅ | All env vars via `secretKeyRef` referencing SecretProviderClass |
| `UNLEASH_API_TOKEN` local-only default (SEC-004) | ✅ | `application.yml` L243 comment documents staging/prod requirement |
| Admin token correctly scoped (LOCAL PROFILE ONLY) | ✅ | Both `INIT_ADMIN_API_TOKENS` and `UNLEASH_TOKEN` carry SEC-004 comment |
| `readOnlyRootFilesystem: true` with `/tmp` emptyDir | ✅ | Spring Boot temp file workaround present |
| `capabilities.drop: [ALL]` | ✅ | Container-level securityContext |
| Trivy CRITICAL blocks publication | ✅ | `exit-code: 1` on CRITICAL severity step |

---

## Metrics

| Metric | Value |
|--------|-------|
| 🔴 Blockers | 0 |
| 🟡 Warnings | 2 (W-001, W-002) |
| 🟢 Suggestions | 2 (S-001, S-002) |
| Prior findings resolved | 10 / 10 (all F-001–F-007 + N-001, N-002, N-003) |
| New findings introduced | 4 (0 blockers, 2 warnings, 2 suggestions) |
| Spec requirements covered | 28 / 28 (100%) |
| Files reviewed | 15 (01-ci.yml, 02-docker.yml, 03-deploy-staging.yml, 04-deploy-production.yml, Dockerfile, checkstyle.xml, docker-compose.yml, k8s/deployment.yaml, k8s/kustomization.yaml, k8s/overlays/*/kustomization.yaml, UnleashFeatureFlagAdapter.java, UnleashClientConfig.java, UnleashFeatureFlagAdapterTest.java, application.yml, quickstart.md) |

---

## Required Actions (Prioritized)

### Fix before merge (Warnings)

**W-001 — `tasks.md` Phase 5 Independent Test**:

Replace `specs/006-cicd-standards/tasks.md` L85:
```markdown
**Independent Test**: `kubectl apply --dry-run=client -k k8s/overlays/staging` → 0 errors, 3 resources configured; no `Secret` YAML present in `k8s/` directory.
```

**W-002 — `plan.md` Étape 5 Verification**:

Replace `specs/006-cicd-standards/plan.md` L306–309:
```bash
kubectl apply --dry-run=client -k k8s/overlays/staging
# Expected: 3 resources configured (no errors)
```

### Nice to fix (Suggestions)

**S-001**: Update T017 description in `tasks.md` to reference `unleashorg/unleash-server:5.13.0` instead of `:latest`.

**S-002**: Update `plan.md` Étape 2 job-mapping table to remove `-DskipPit` from the sonarcloud row and align it with the live `01-ci.yml`.

---

## Verdict

⚠️ **APPROVED WITH CONDITIONS**

All 10 prior findings are resolved. FR-019 is now fully implemented — 28/28 spec requirements satisfied. No new blockers. Two documentation warnings (W-001, W-002) contain stale `kubectl apply -f k8s/` commands that will fail if executed, since `k8s/kustomization.yaml` now exists. Fix these two lines, then proceed to `/speckit.ship`.

**Conditions for final APPROVED:**
1. Fix W-001: update `tasks.md` Phase 5 Independent Test from `-f k8s/` to `-k k8s/overlays/staging`
2. Fix W-002: update `plan.md` Étape 5 Verification from `-f k8s/` to `-k k8s/overlays/staging`
